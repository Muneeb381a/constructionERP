# Leave unset for local dev — Vite's proxy (vite.config.ts) forwards /api to localhost:4000.
# Set this when the frontend and API are deployed as separate Vercel projects, to the API
# project's full base URL, e.g. https://construction-erp-api.vercel.app/api
VITE_API_URL=
